import React from "react";
import { shallow } from "enzyme";
import PostGrid from './PostGrid';

describe('PostGrid', () => {
  it.todo('should display posts as cards', () => {
    const wrapper = shallow(<PostGrid posts={[]} />);


  });
});
